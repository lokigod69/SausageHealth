# Shared intake pilot · next step

## FOR YOU

The refined island workspace is running locally, the adversarial review is saved, and source `68b633f` passed 26 tests plus Docker build/runtime checks in private GitHub CI. Restart future Windows sessions with `npm run start:local`. The proposed address is `ops.thesausageguy.shop`; the existing public site's DNS points to Porkbun/Vercel. Confirm the hostname and hosting target, then provide the appropriate private access and Moritz's account email. No second domain purchase is needed for a subdomain.

Before sharing the pilot, verify the chosen host's HTTPS, individual accounts, Moritz's actual phone upload/review, restart persistence, and off-host backup/restore. Moritz's first task is one recent daily sales source from each store, then a receipt. The app and `docs/MORITZ_START_HERE.md` guide him. See `docs/ADVERSARIAL_REVIEW.md` for the remaining integration gates.

## PASTE THIS

Resume Sausage Health from `protocol/PROTOCOL.md`, `memory/INDEX.md`, `memory/STATE.md`, and the latest protocol log. Read `docs/ADVERSARIAL_REVIEW.md` for the open gates. Preserve the owner's liked island and Tropical Observatory refinements; keep related repositories read-only. If needed, restart the local preview with `npm run start:local`. `ops.thesausageguy.shop` is proposed, not selected/deployed; no hosting access or Moritz account details have been supplied. When the owner selects the host/hostname and authorizes access/deployment, follow `docs/DEPLOYMENT.md`, preserve the public site's root/www/email DNS, build/test on the target, create individual accounts, verify actual HTTPS and phone upload/review/restart, and perform a protected off-host backup/restore drill before announcing the shared intake pilot. If business files arrive first, preserve originals through the private collection and review ambiguities without inventing facts. Source review is not accounting reconciliation. AI stays disabled until prompt/schema run/cache versioning, proposal review, provider/model/budget and representative evaluation are handled; Loyverse needs live read-only verification. No spending, customer actions, live-stock or profitability claims from generic autopilot authority. Complete only the requested milestone and save durable state plus the exact next action at its checkpoint.
