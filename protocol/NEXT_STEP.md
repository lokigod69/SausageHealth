# Shared intake pilot · next step

## FOR YOU

The invitation notice and decorative copy are removed. The island stays. Source `87c802d` passed all 26 tests and Docker checks in private GitHub CI. The preview is running at http://127.0.0.1:5180; restart future sessions with `npm run start:local`.

Vercel is selected and its `sausage-health` project is created. Moritz's contact is saved and his records request was already sent. **Next: sign into Vercel and review the [Neon Free database terms](https://vercel.com/lokigod69s-projects/~/integrations/accept-terms/neon?source=cli).** The database is not connected yet; the app still runs locally. Then adapt/test private cloud storage, deploy login/uploads, and complete phone/backup checks. No server IP is needed.

## PASTE THIS

Resume from protocol, memory and the latest publication/CI log. Preserve Tropical Observatory's island and plain functional copy; no motivational/AI slogans, invitation notices, unsolicited coaching or generic guide dumps. The owner selected Vercel for `ops.thesausageguy.shop`; its separate `sausage-health` project is created/linked under `lokigod69s-projects`. Do not ask for provider/IP. Read `docs/VERCEL.md` before implementation: Neon Free setup stopped at owner sign-in/terms acceptance; the waiting CLI was canceled, and resource listing returned none. Once accepted, inspect first and resume setup without duplicates. Private Blob is only proposed; do not select public storage or silently upgrade plans. Current SQLite/local uploads cannot run unchanged on Vercel; implement/test cloud persistence, bounded authenticated direct uploads, immutable finalization/idempotency, authorized large downloads and consistent Postgres+files off-provider backup/restore. Preserve the local/container baseline and read-only related repositories. Do not publish a static login with no working API. Configure actual HTTPS, individual accounts, real phone upload/review and redeployment persistence, then complete restore acceptance before announcing shared intake. Moritz's contact and already-sent records email remain private in `.data/moritz-handoff.json`; do not resend. AI and Loyverse are not connected: configured provider/model/budget/sample evaluation and prompt/schema/proposal review gates remain; no financial/live-stock claims or customer/spending actions from autopilot. If business files arrive first, preserve originals/provenance and update only supported facts. Explain the system in a few plain sentences when asked. Save the exact next action at the next meaningful checkpoint.
