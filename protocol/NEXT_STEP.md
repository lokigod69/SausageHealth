# Shared intake pilot · next step

## FOR YOU

The invitation notice and decorative copy are removed. The island stays. Source `87c802d` passed all 26 tests and Docker checks in private GitHub CI. The preview is running at http://127.0.0.1:5180; restart future sessions with `npm run start:local`.

`ops.thesausageguy.shop` is confirmed. Moritz's email is saved privately, and the first-records request was sent. His online login is not ready. **Next: identify the server and provide access, or approve a concrete new server configuration and cost.** Then configure the domain, accounts and HTTPS, verify his phone upload/review and test off-host backup/restore before sharing the pilot.

## PASTE THIS

Resume from `protocol/PROTOCOL.md`, `memory/INDEX.md`, `memory/STATE.md` and the latest protocol log. Preserve Tropical Observatory's original island and the owner's explicit plain-copy correction: functional labels, no motivational/AI slogans, no invitation notices or unsolicited coaching. Give concrete next actions, not generic guide links. Keep related repositories read-only. Restart the local preview with `npm run start:local` if needed. `ops.thesausageguy.shop` is confirmed; Moritz's email and the verified sent message are in private Git-ignored `.data/moritz-handoff.json`. Do not request those details again or resend the initial records email. No hosted account/deployment exists. Next obtain the actual hosting server/access, or approval of a concrete new server configuration/cost. Then follow `docs/DEPLOYMENT.md`: preserve root/www/email DNS, build/test the target container, create individual accounts, verify HTTPS and real phone upload/review/restart, and perform a protected off-host backup/restore drill before announcing shared intake. If business files arrive first, preserve private originals/provenance and review ambiguities; update only supported facts. Source review is not accounting reconciliation. AI remains disabled pending prompt/schema run/cache versioning, proposal review, configured provider/model/budget and sample evaluation. Loyverse needs live read-only verification. No spending, customer actions, live-stock or profitability claims from generic autopilot authority. Finish only the requested milestone and save the exact next action.
